---
- name: Delete autogenerated PTR records for each IP
  include_tasks: delete_ptr_for_ip.yml
  loop: "{{ a_record.records | default([]) }}"
  loop_control:
    loop_var: ip_address
  when: ip_address is ansible.utils.ipv4
