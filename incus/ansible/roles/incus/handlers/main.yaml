---
- name: Update apt
  ansible.builtin.apt:
    force_apt_get: true
    update_cache: true
    cache_valid_time: 0

- name: Restart systemd-resolved
  ansible.builtin.systemd:
    name: systemd-resolved
    state: restarted
    daemon_reload: yes
